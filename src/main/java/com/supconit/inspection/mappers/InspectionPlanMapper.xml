<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.supconit.inspection.entities.InspectionPlan" >
	<resultMap id="inspectionPlanResultMap" type="com.supconit.inspection.entities.InspectionPlan" >
		<result property="id" column="ID" />
	    <result property="deviceId" column="DEVICE_ID"/>
	    <result property="inspectionContentId" column="INSPECTION_CONTENT_ID"/>
	    <result property="planDate" column="PLAN_DATE"/>
	    <result property="month" column="MONTH"/>
	    <result property="deviceName" column="DEVICE_NAME"/>
	    <result property="locationName" column="LOCATION_NAME"/>
	    <result property="itemContent" column="ITEM_CONTENT"/>
	    <result property="inspectionGroupName" column="INSPECTION_GROUP_NAME"/>
	    <result property="cycle" column="CYCLE"/>
	    <result property="cycleUnit" column="CYCLE_UNIT"/>
	    <result property="remark" column="REMARK"/>
	</resultMap>
	
	<update id="readyForPlan">
  		{call proc_inspection_plan()}
	</update>
	
	<sql id="TABLE_NAME" lang="velocity">INSPECTION_PLAN</sql>
	
	<sql id="select_pre" lang="velocity">
		SELECT
			a.*,
			d.DEVICE_NAME,
			d.LOCATION_NAME,
			ii.ITEM_CONTENT,
			ii.INSPECTION_GROUP_NAME,
			ii.CYCLE,
			ii.CYCLE_UNIT,
			ii.REMARK
		FROM <include refid="TABLE_NAME"/> a
		LEFT JOIN DEVICE d ON a.DEVICE_ID = d.ID
		LEFT JOIN INSPECTION_ITEM ii ON a.INSPECTION_CONTENT_ID = ii.ID
	</sql>
	
	<sql id="count_pre" lang="velocity">
		SELECT COUNT(*) 
		FROM <include refid="TABLE_NAME"/> a 
		LEFT JOIN DEVICE d ON a.DEVICE_ID = d.ID
	</sql>
	
	<sql id="condition_sql" lang="velocity">
		<![CDATA[
		#where()
			#set($_p = $_parameter.condition)
			#if($_p.deviceCode && $_p.deviceCode != '')
				#set($_deviceCode = '%' + $_p.deviceCode + '%')
				AND d.DEVICE_CODE LIKE @{_deviceCode} ESCAPE '/' 
			#end
			#if($_p.deviceName && $_p.deviceName != '')
				#set($_deviceName = '%' + $_p.deviceName + '%')
				AND d.DEVICE_NAME LIKE @{_deviceName} ESCAPE '/' 
			#end
			#if($_p.year && $_p.month)
				#set($_year = $_p.year)
				#set($_month = $_p.month)
				AND a.YEAR = @{_year,jdbcType=INTEGER}
				AND a.MONTH = @{_month,jdbcType=INTEGER}
			#end
			#if($_p.week)
				#set($_week = $_p.week)
				AND a.WEEK = @{_week,jdbcType=INTEGER}
			#end
			#if($_p.beginTime && $_p.beginTime != '')
				#set($_beginTime = $_p.beginTime)
				AND a.PLAN_DATE >= @{_beginTime,jdbcType=TIMESTAMP}
			#end
			#if($_p.endTime && $_p.endTime != '')
				#set($_endTime = $_p.endTime)
				AND a.PLAN_DATE < @{_endTime,jdbcType=TIMESTAMP}
			#end
			#if($_p.deviceCategoryChildIds && $_p.deviceCategoryChildIds.size()>0)
				#set($_deviceCategoryChildIds =$_p.deviceCategoryChildIds)
				AND 
				#repeat( $_deviceCategoryChildIds $id "," " d.CATEGORY_ID IN (" ")" )
					@{id,jdbcType=BIGINT}
				#end
			#end
			#if($_p.geoAreaChildIds && $_p.geoAreaChildIds.size()>0)
				#set($_geoAreaChildIds =$_p.geoAreaChildIds)
				AND 
				#repeat( $_geoAreaChildIds $id "," " d.LOCATION_ID IN (" ")" )
					@{id,jdbcType=BIGINT}
				#end
			#end
		#end
		]]>	
    </sql>
    
	<sql id="select_pre_month" lang="velocity">
		SELECT 
		   t.*,
	       t3.DEVICE_NAME,
	       t3.DEVICE_CODE,
	       t3.LOCATION_NAME,
	       t2.ITEM_CONTENT,
	       t2.INSPECTION_GROUP_NAME,
	       t2.REMARK
		  FROM (SELECT a.DEVICE_ID,
		               a.INSPECTION_CONTENT_ID,
		               COUNT(DECODE(WEEK, 1, 1)) AS "firstWeek",
		               COUNT(DECODE(WEEK, 2, 1)) AS "secondWeek",
		               COUNT(DECODE(WEEK, 3, 1)) AS "thirdWeek",
		               COUNT(DECODE(WEEK, 4, 1)) AS "fourWeek"
		   FROM <include refid="TABLE_NAME"/> a
		   LEFT JOIN DEVICE d ON a.DEVICE_ID = d.ID
	       LEFT JOIN INSPECTION_ITEM ii ON a.INSPECTION_CONTENT_ID = ii.ID      
	</sql>
	
	<sql id="count_pre_month" lang="velocity">
	SELECT COUNT(DISTINCT(a.INSPECTION_CONTENT_ID)) 
	FROM <include refid="TABLE_NAME"/> a 
	LEFT JOIN DEVICE d ON a.DEVICE_ID = d.ID
	</sql>
	
	<!-- 按条件查询记录-->
    <select id="findByConditions" parameterType="com.supconit.inspection.entities.InspectionPlan" resultMap="inspectionPlanResultMap" lang="velocity" databaseId="oracle">
        <include refid="_PUBLIC_V.PAGER_BEFORE"/>
		<include refid="select_pre"/>
		<include refid="condition_sql"/>
		#order($_parameter.condition,"a.DEVICE_ID DESC")
		<include refid="_PUBLIC_V.PAGER_AFTER"/>
    </select>
    
	<!-- 按条件查询记录-->
    <select id="findByConditions" parameterType="com.supconit.inspection.entities.InspectionPlan" resultMap="inspectionPlanResultMap" lang="velocity" databaseId="sqlserver">
        <include refid="_PUBLIC_V.PAGER_BEFORE"/>
		SELECT
			a.*,
			d.DEVICE_NAME,
			d.LOCATION_NAME,
			ii.ITEM_CONTENT,
			ii.INSPECTION_GROUP_NAME,
			ii.CYCLE_UNIT, 
			ii.REMARK
		<include refid="_PUBLIC_V.ROW_NUMBER_OVER" /> 
		#order($_parameter.condition,"a.DEVICE_ID DESC")
		<include refid="_PUBLIC_V.AS_ROW_NUM" />
		FROM <include refid="TABLE_NAME"/> a
		LEFT JOIN DEVICE d ON a.DEVICE_ID = d.ID
		LEFT JOIN INSPECTION_ITEM ii ON a.INSPECTION_CONTENT_ID = ii.ID
		<include refid="condition_sql"/>
		<include refid="_PUBLIC_V.PAGER_AFTER"/>
    </select>

    <select id="countByConditions" resultType="long" lang="velocity">
        <include refid="count_pre"/>
        <include refid="condition_sql"/>
    </select>
    
	<!-- 按条件查询记录-->
    <select id="findByConditionsMonth" parameterType="com.supconit.inspection.entities.InspectionPlan" resultMap="inspectionPlanResultMap" lang="velocity" databaseId="oracle">
        <include refid="_PUBLIC_V.PAGER_BEFORE"/>
		<include refid="select_pre_month"/>
		<include refid="condition_sql"/>
		GROUP BY a.DEVICE_ID, a.INSPECTION_CONTENT_ID) t
		 INNER JOIN INSPECTION_ITEM t2
		    ON t.INSPECTION_CONTENT_ID = t2.ID
		 INNER JOIN DEVICE t3
		    ON t.DEVICE_ID = t3.ID
		#order($_parameter.condition,"t.DEVICE_ID DESC")
		<include refid="_PUBLIC_V.PAGER_AFTER"/>
    </select>
    
	<!-- 按条件查询记录-->
    <select id="findByConditionsMonth" parameterType="com.supconit.inspection.entities.InspectionPlan" resultMap="inspectionPlanResultMap" lang="velocity" databaseId="sqlserver">
        <include refid="_PUBLIC_V.PAGER_BEFORE"/>
		SELECT 
		   t.*,
	       t3.DEVICE_NAME,
	       t3.DEVICE_CODE,
	       t3.LOCATION_NAME,
	       t2.ITEM_CONTENT,
	       t2.INSPECTION_GROUP_NAME,
	       t2.REMARK
		  FROM (SELECT a.DEVICE_ID,
		               a.INSPECTION_CONTENT_ID,
		               COUNT(DECODE(WEEK, 1, 1)) AS "firstWeek",
		               COUNT(DECODE(WEEK, 2, 1)) AS "secondWeek",
		               COUNT(DECODE(WEEK, 3, 1)) AS "thirdWeek",
		               COUNT(DECODE(WEEK, 4, 1)) AS "fourWeek"
		<include refid="_PUBLIC_V.ROW_NUMBER_OVER" /> 
		#order($_parameter.condition,"t.DEVICE_ID DESC")
		<include refid="_PUBLIC_V.AS_ROW_NUM" />
		FROM <include refid="TABLE_NAME"/> a
		LEFT JOIN DEVICE d ON a.DEVICE_ID = d.ID
	    LEFT JOIN INSPECTION_ITEM ii ON a.INSPECTION_CONTENT_ID = ii.ID
		<include refid="condition_sql"/>
		GROUP BY a.DEVICE_ID, a.INSPECTION_CONTENT_ID) t
		 INNER JOIN INSPECTION_ITEM t2
		    ON t.INSPECTION_CONTENT_ID = t2.ID
		 INNER JOIN DEVICE d
		    ON t.DEVICE_ID = d.ID
		<include refid="_PUBLIC_V.PAGER_AFTER"/>
    </select>

    <select id="countByConditionsMonth" resultType="long" lang="velocity">
        <include refid="count_pre_month"/>
        <include refid="condition_sql"/>
    </select>
    
</mapper>